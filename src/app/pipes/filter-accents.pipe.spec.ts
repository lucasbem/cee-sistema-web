import { FilterAccentsPipe } from './filter-accents.pipe';

describe('FilterAccentsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAccentsPipe();
    expect(pipe).toBeTruthy();
  });
});
