import { FilterEnhancedPipe } from './filter-enhanced.pipe';

describe('FilterEnhancedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEnhancedPipe();
    expect(pipe).toBeTruthy();
  });
});
