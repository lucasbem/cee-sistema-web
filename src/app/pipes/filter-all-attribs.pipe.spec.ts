import { FilterAllAttribsPipe } from './filter-all-attribs.pipe';

describe('FilterAllAttribsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAllAttribsPipe();
    expect(pipe).toBeTruthy();
  });
});
